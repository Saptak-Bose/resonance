import TextToSpeechDetailSkeleton from "@/features/text-to-speech/components/text-to-speech-detail-skeleton";

type Props = object;

export default function LoadingPage({}: Props) {
  return <TextToSpeechDetailSkeleton />;
}
